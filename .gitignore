node_modules
dist
.env.local
.env.*
!.env.example
!.env.*.example
.DS_Store

# DB backups (contain user data — never commit)
scripts/.backup-env
backups/

# Local staging/admin state (contains credentials or project links)
.supabase-local/
supabase/.temp/
